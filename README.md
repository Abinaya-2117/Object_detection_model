# Object_detection_model
This project implements an object detection system inspired by Tesla Autopilot using the YOLOv8 deep learning model. The goal is to detect and localize objects such as cars, pedestrians, and cyclists in road scenes. The model was trained on the KITTI dataset, which contains real-world driving images with labeled bounding boxes.
